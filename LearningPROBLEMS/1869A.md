
here i want to add a method to solve the question ,where the case of minimum operations are implemented.
```cpp
#include <bits/stdc++.h>

using namespace std;
void solve(int n, vector<int>& a) {
    bool allZero = true;
    int xr = 0;

    for (int x : a) {
        xr ^= x;
        if (x != 0)
            allZero = false;
    }

    if (allZero)
        cout<<0;
else
    if (xr == 0)
        cout<<1<<endl<<1<<" "<<n;
else
    if (n % 2 == 0)
       cout<<2<<endl<<1<<" "<<n<<endl<<1<<" "<<n;
else 
    cout<<4<<endl<<1<<" "<<n-1<<endl<<1<<" "<<n-1<<endl<<n-1<<" "<<n
<<endl<<n-1<<" "<<n;
    
}

int main() {
        int t;
        cin >> t;
        while (t--) {
            int n;
            cin >> n;
            vector < int > v(n);
            for (auto & x: v) cin >> x;
solve(n,v);
cout << endl;
            }

        }
```
