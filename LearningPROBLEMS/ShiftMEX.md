
# vec.erase(unique(vec.begin(), vec.end()), vec.end());
means:

1.Move unique consecutive elements to the front, and return an iterator after the last unique element

2.Delete the remaining duplicate elements.
```cpp
{1,2,1,3,4,1,2} //do the sorting first
then this code   -> {1,2,3,4}
```
```cpp

```
